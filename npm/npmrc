# linux reads npm config from ~/.config/npm/npmrc
registry=https://registry.npmmirror.com
cache=${HOME}/.npm-cache
# prefix=${HOME}/.npm-global # disable this in npmrc, use system_variable `NPM_CONFIG_PREFIX` instead
