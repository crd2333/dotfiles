{
  "rules": [
    { "match": { "provider": "aio", "modelIdRegex": "^claude-" }, "template": "claude-code.headers" },
    { "match": { "provider": "aio", "modelIdRegex": "^gpt-" }, "template": "codex.headers" }
  ],
  "blacklist": ["Authorization", "Content-Length", "Host", "Content-Encoding", "Connection", "Accept-Encoding"],
  "sandbox": "seccomp",
  "debug": false
}
